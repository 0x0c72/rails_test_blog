<br />
<br />
<br />
<p>
	<h1><%= @article.title %></h1>
</p>
<p>
	<strong>Text:</strong>
	<%= @article.text %>
</p>

<h2>Comments</h2>
<%= render @article.comments if @article.comments.size > 0 %>
<% if @article.comments.size == 0 %>
	<p>No Comments to Display</p>
<% end %>
<h2>Add a comment:</h2>
<%= render "comments/form" %>

<%= link_to 'Back', articles_path %>
| <%= link_to 'Edit', edit_article_path(@article) %>
